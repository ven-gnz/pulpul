describing subprogram called test acting on inputs str1, str2 producing outputs:
    return "testing return value with multistring " + "another string " + "parameter 1 " + str1 + "parameter 2 " + str2 + " there we go!";
    .




display result test("abc","kiria"), "testing multi print after multi string " + "pekka", "pouta", "suomen säämies"



